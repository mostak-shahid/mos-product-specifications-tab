Update Npm packages

npm install -g npm-check-updates
ncu -u
npm install


composer global require afragen/wp-devops


wp package install wp-cli/dist-archive-command:@stable
wp dist-archive wp-content/plugins/mos-product-specifications-tab mos-product-specifications-tab-v1.0.0.zip --plugin-dirname=mos-product-specifications-tab --filename-format='{slug}-{version}.zip'


Final: go to the plugin folder
# Clean vendor directory
rm -rf vendor

# Install prod-only dependencies
composer install --no-dev --optimize-autoloader

# Create the distribution zip
wp dist-archive .
